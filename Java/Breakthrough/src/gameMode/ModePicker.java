package gameMode;

public class ModePicker {
    public GameMode PickMode(String mode){
        if(mode=="Breakthrought")
            return new Breakthrought();
        else
        return new Breakthrought();
    }
}
